interface:
  display_name: "Ask Skills"
  short_description: "Route to the smallest composable skill flow."
  default_prompt: "Use $ask-skills to choose the smallest local flow; for canonical ticket Markdown, resolve ticket-autopilot from the catalog, run ticket-parse, and pass its normalized output plus the runner CandidateRef to execute-ticket."
