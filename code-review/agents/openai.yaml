interface:
  display_name: "Code Review"
  short_description: "Review semantics, evidence, and release claims."
  default_prompt: "Use $code-review to review this diff for external-boundary regressions, ticket compliance, causal evidence gaps, and overclaims."
