interface:
  display_name: "Code Review"
  short_description: "Review a diff for standards and spec/ticket compliance."
  default_prompt: "Use $code-review to review these changes against the relevant spec or ticket."