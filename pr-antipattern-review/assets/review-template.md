# PR Review — <PR title / commit SHA / branch name>

Reviewed on <YYYY-MM-DD>.
Source: <PR URL / commit SHA / local diff>.
Blueprint: <path loaded, or "none — running with universal checks only">.
Scope: <N files changed, +X / −Y lines>.

## Verdict

<✅ LGTM | ✅ LGTM with notes | ⚠️ Comment | ❌ Request changes>

<One-sentence summary of why.>

## Summary

| Tier | arch | sec | config | api | test | Total |
|---|---|---|---|---|---|---|
| Blocker | 0 | 0 | 0 | 0 | 0 | **0** |
| Should-fix | 0 | 0 | 0 | 0 | 0 | **0** |
| Nit | 0 | 0 | 0 | 0 | 0 | **0** |

## What this PR does right

<1-3 things worth calling out — paying down debt, following slice conventions, adding tests, etc. Balances the review and proves the reviewer verified.>

- <observation>

## Findings

Grouped by **slice** when comparison is slice-based; by **axis** when running universal checks only.

### Slice: <slice name>

**`<path>:<line>`** — [blocker | should-fix | nit] · `<axis>`

<One-line description of the problem.>

> ```language
> <offending line from diff>
> ```

**Slice constraint violated** (if applicable): <which constraint from the blueprint>

**Direction**: <one or two sentences referencing the slice's canonical or the rule>

---

<repeat per finding, grouped by slice>

### Universal: <axis>

**`<path>:<line>`** — [blocker | should-fix | nit] · `<axis>`

<description>

> ```language
> <line>
> ```

**Direction**: <one sentence>

---

## Pre-existing context (not held against this PR)

<Optional section. Up to 2-3 significant pre-existing problems in touched files for awareness only. Skip if nothing notable.>

- `<path>` <description>. This PR doesn't worsen it.

## Trajectory

<One sentence: does this PR move the codebase forward, sideways, or backward?>

Examples:
- *"Forward: introduces tests for a previously-untested service, pays down two ports from the application layer into the domain layer."*
- *"Sideways: bugfix is correct but perpetuates a legacy style instead of using the project's current canonical."*
- *"Backward: introduces an undocumented variant of an existing slice, removes an auth validation step, hardcodes an external endpoint. Recommend rework before merge."*

---

*Generated by `pr-antipattern-review` skill. Findings are ephemeral; this output lives in chat unless explicitly saved.*
