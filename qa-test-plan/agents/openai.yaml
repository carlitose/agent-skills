interface:
  display_name: "QA Test Plan"
  short_description: "Plan causal QA with evidence and HITL gates."
  default_prompt: "Use $qa-test-plan to create a manual QA plan that maps boundary changes to evidence classes, causal coverage, and HITL gates."
