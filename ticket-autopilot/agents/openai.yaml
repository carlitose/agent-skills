interface:
  display_name: "Ticket Autopilot"
  short_description: "Schedule and finalize a deterministic ticket folder."
  default_prompt: "Use $ticket-autopilot as the concise folder scheduler: parse canonical envelopes, delegate one-ticket loops, enforce gates, and finalize one PR per ticket without auto-merge."
