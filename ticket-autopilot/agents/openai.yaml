interface:
  display_name: "Ticket Autopilot"
  short_description: "Run bounded, resumable ticket leaves with exact gates."
  default_prompt: "Use $ticket-autopilot as the concise folder scheduler: parse canonical envelopes, enforce separate quality and leaf budgets, persist schema-3 partial handoffs, and finalize one PR per ticket without auto-merge."
